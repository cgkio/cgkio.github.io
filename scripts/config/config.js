'use strict';
// Declare app level module which depends on filters, and services
angular.module('myApp.config', []).constant('version', '0.6').constant('loginRedirectPath', '/login').constant('FBURL', 'https://cgkio.firebaseio.com');